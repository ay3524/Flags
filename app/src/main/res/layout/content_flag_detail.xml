<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="ay3524.com.flags.ui.FlagDetail"
    tools:showIn="@layout/activity_flag_detail">

    <ImageView
        android:id="@+id/flag_image"
        android:layout_width="@dimen/image_detail_width_dimen"
        android:layout_height="@dimen/image_detail_height_dimen"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:layout_margin="@dimen/image_detail_margin"
        android:background="@drawable/border"
        android:scaleType="fitXY" />

    <TextView
        android:id="@+id/rank"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/flag_image"
        android:layout_centerInParent="true"
        android:textColor="@android:color/black" />

    <TextView
        android:id="@+id/country"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/rank"
        android:layout_centerInParent="true"
        android:textColor="@android:color/black" />

    <TextView
        android:id="@+id/population"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/country"
        android:layout_centerInParent="true"
        android:textColor="@android:color/black" />

</RelativeLayout>
